name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: config
      run: ./configure
    - name: make 
      run: make -C STEPin_Mini_Project/3_Implementation/

